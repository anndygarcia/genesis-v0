{
  "name": "genesis-pipeline",
  "version": "1.0.0",
  "description": "Genesis async extraction pipeline (Railway.app).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "better-sqlite3": "^11.0.0",
    "uuid": "^9.0.1",
    "pdf-lib": "^1.17.1"
  },
  "//": "pdfjs-dist, canvas, onnxruntime-node installed by Dockerfile (native build deps)",
  "engines": {
    "node": ">=20"
  }
}